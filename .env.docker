# Backend settings for Docker environment
HOST_BACKEND=0.0.0.0
PORT_BACKEND=8000

# PostgreSQL direct settings for Docker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=events_db
HOST_POSTGRES=postgres
PORT_POSTGRES=5432

# Direct connection URL without variable substitution
CONNECTION_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/events_db 